next-version: 2.22.0
tag-prefix: '(v|V|eth-v)'
branches:
  main:
    regex: ^main$|^etherna/main$
  etherna:
    regex: ^etherna/v
    mode: ContinuousDeployment
    tag: eth
    increment: None
    source-branches: [ 'main' ]
